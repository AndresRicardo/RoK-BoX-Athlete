VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Web Push (VAPID) - solo la publica va al cliente.
# Genera el par con: npx web-push generate-vapid-keys
# La privada va a Supabase: supabase secrets set VAPID_PRIVATE_KEY=...
VITE_VAPID_PUBLIC_KEY=
